@model IEnumerable<Capa_Logica_Negocio.Models.CreditoConsultaModel>

@if (Model.Count() != 0)
{
    <table class="table table-borderless table-sm table-hover mb-2">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model=> model.Decripcion_Credito_Consulta)
                </th>
                <th>
                    @Html.DisplayNameFor(model=> model.Fecha_Credito_Consulta)
                </th>
                <th>
                    @Html.DisplayNameFor(model=> model.Monto_Credito_Consulta)
                </th>
            </tr>
        </thead>
        <tbody >

            @foreach (var item in @Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem=> item.Decripcion_Credito_Consulta)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem=> item.Fecha_Credito_Consulta)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem=> item.Monto_Credito_Consulta)
                    </td>

                </tr>

            }


        </tbody>


    </table>
}
else
{
    <div class="alert alert-warning" role="alert">
        Sin registros
    </div>
}




